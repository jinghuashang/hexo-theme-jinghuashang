<% if (page.total > 1){ %>
  <nav class="paginator">
    <%- paginator({
      prev_text: '<svg viewBox="0 0 7 15" width="7" height="15"><polygon points="7,0 0,7.5 7,15" fill="currentColor"/></svg> ' + __('Paginator.Prev'),
      next_text: __('Paginator.Next') + ' <svg viewBox="0 0 7 15" width="7" height="15"><polygon points="0,0 7,7.5 0,15" fill="currentColor"/></svg>'
    }) %>
  </nav>
<% } %>
